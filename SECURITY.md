# Security policy

Do not open a public issue containing tokens, passwords, private keys, panel
snapshots or subscription links. Revoke an exposed credential first, then use a
private GitHub security advisory.

Mutation, unattended repair and automatic rollback are disabled by default.
Test plans and rollback on an isolated deployment before enabling them.
